@model WebPartyTutoriol.Models.GuestResponse

@{
    ViewBag.Title = "Thanks";
}
@{ 
    try
    {
        WebMail.SmtpServer = "smtp.gmail.com";
        WebMail.SmtpPort = 587;
        WebMail.EnableSsl = true;
        WebMail.UserName = "ifokhrul09@gmail.com";
        WebMail.Password = "*********";
        WebMail.From = "rsvps@example.com";

        WebMail.Send("ifokhrul09@gmail.com", "RSVP notification", Model.Name + "is " + ((Model.WillAttend ?? false) ? "" : "not") + " attending");
    }
    catch (Exception)
    {
        <b>Sorry - we couldnt send the email to confirm your RSVP.</b>
    }
}
<div class="text-center">
    <div class="lead">

        <h1>Thank you, @Model.Name</h1>

        @if (Model.WillAttend == true)
        {
            @:It's great that your coming!
}
        else
        {
            @:Sorry to hear that you can't make it, but thanks for letting us know.
}
    </div>
</div>
